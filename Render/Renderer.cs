using System;
namespace fraction_calculator_dotnet.Render
{
    public class Renderer
    {
        private Mode _mode;

        public Renderer(Mode mode)
        {
            _mode = mode;
        }
    }
}
